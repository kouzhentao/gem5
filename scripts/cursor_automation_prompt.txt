# Paste into Cursor → Automations → New → Schedule (every 5 min)
# Repo: /home/kou/gem5

Read ANDES_STATUS.txt (FOCUS) and ANDES_WORK_QUEUE.md (first TODO in NEXT table).

Do exactly ONE queue item per run:
- P4-doc-* : extend docs/ax46mpv_issue_hazard.md (mark §7 row done)
- OR RTL → 3-line rule in docs/andes_46mpv_rtl_rules.md
- OR gem5 map for existing rule

Then update ANDES_WORK_QUEUE.md (advance NEXT), ANDES_STATUS.txt (FOCUS=next id), append ANDES_STATUS.log.

First line of reply: Status: <one-line summary>.

Forbidden: exlx A/B impl, brief-only status, BP/cfg re-audit, opLat/issueLimit tuning, gauge-hold without doc progress.

Truth order: cfg.txt → DS238 → ucore RTL.
Doc-first: hazard/issue/bypass in ax46mpv_issue_hazard.md before 8-stage gem5.

Current FOCUS: see ANDES_STATUS.txt (P4-doc-02 ctrl reg fields).
