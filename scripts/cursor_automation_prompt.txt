# Paste into Cursor → Automations → New → Schedule (every 5 min)
# Repo: this workspace (/home/kou/gem5). Cloud agent recommended.

Read ANDES_STATUS.txt (FOCUS) and ANDES_WORK_QUEUE.md (NEXT row).

Do exactly ONE queue item per run:
- RTL read → append one 3-line rule block to docs/andes_46mpv_rtl_rules.md
- OR implement gem5 mapping for an existing rule in that file

Then update ANDES_WORK_QUEUE.md (advance NEXT), ANDES_STATUS.txt, append ANDES_STATUS.log.

First line of reply: Status: <one-line summary> (not "Loop tick #").

Forbidden: brief-only status, BP re-audit, opLat/issueLimit tuning, gauge-hold without artifact.
CoreMark ~6.3 is ruler only, not a tuning target.

Truth order: cfg.txt → DS238 → ucore RTL (kv_iiu_scb, kv_ipipe, kv_lsu*, kv_core).
Do NOT use andes_vip ax45mpv_core_top sample localparams.

Current NEXT is usually P3-gem5-ex-lx (EX∥LX stage model, ALU stays 1cy).
